# Example requests

- Use `$image-to-editable-pptx` to rebuild this architecture-diagram screenshot as one editable slide. Preserve all labels and connector directions.
- Use `$image-to-editable-pptx` to convert these four rendered slide images into a four-slide PPTX in the same order. Keep photos as cropped images and rebuild all readable text.
- Use `$image-to-editable-pptx` to reproduce this dashboard image. Keep tables and charts editable, and report any region that must remain rasterized.

The Skill should not activate for a request to create a new presentation from an outline when there is no image reference.
